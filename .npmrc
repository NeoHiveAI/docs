# starlight-contextual-menu (0.1.5) declares a peer of astro ^5, but it runs
# fine on the Astro 7 stack. Tolerate that lagging peer so `npm ci` (CI) and
# `npm install` (local) resolve identically. Revisit if the plugin updates its
# peer range.
legacy-peer-deps=true
